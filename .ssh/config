Host *
  # Enable persistent connection multiplexing
  ControlMaster auto
  ControlPath ~/.ssh/-%r@%h:%p
  ControlPersist 600

  # Automatically add keys on first use
  AddKeysToAgent yes

Include ~/.ssh/localconfig
